// Cloudflare vendor helpers (R2-backed FileSystem, D1 binding adapters,
// Workers AI integration, etc.).
//
// Placeholder: separate from the workerd runtime layer at /worker.
// This module will host Cloudflare-specific bindings as we add them.

export {}
